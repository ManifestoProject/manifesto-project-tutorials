job1:
  script:
  - echo $MANIFESTO_APIKEY > manifesto_apikey.txt
  - R -e "library(withr); library(manifestor); library(bookdown); rmarkdown::render_site(output_format = 'bookdown::gitbook', encoding = 'UTF-8')"
  artifacts:
    paths:
    - _book/
